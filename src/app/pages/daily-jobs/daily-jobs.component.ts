import {Component,TemplateRef} from '@angular/core';import {HttpClient} from '@angular/common/http';import {NgbModal} from '@ng-bootstrap/ng-bootstrap';import Swal from 'sweetalert2';import {environment} from '../../../environments/environment';
@Component({selector:'app-daily-jobs',templateUrl:'./daily-jobs.component.html',standalone:false})export class DailyJobsComponent{jobs:any[]=[];schedules:any[]=[];pickups:any[]=[];selectedJob:any=null;schedule_id='';job_date=new Date().toISOString().slice(0,10);constructor(private http:HttpClient,private modal:NgbModal){}ngOnInit(){this.load()}load(){this.http.get<any>(`${environment.apiUrl}/daily-jobs`).subscribe(r=>this.jobs=r.data);this.http.get<any>(`${environment.apiUrl}/schedules`).subscribe(r=>this.schedules=r.data)}generate(){this.http.post<any>(`${environment.apiUrl}/daily-jobs`,{schedule_id:this.schedule_id,job_date:this.job_date}).subscribe({next:()=>{this.load();void Swal.fire('Generated','Daily job generated successfully.','success')},error:e=>void Swal.fire('Generation failed',e.error?.message??'Check schedule and date.','error')})}openPickups(job:any,content:TemplateRef<unknown>){this.selectedJob=job;this.pickups=[];this.modal.open(content,{size:'xl',scrollable:true});this.http.get<any>(`${environment.apiUrl}/daily-jobs/${job.id}/todays-pickups`).subscribe({next:r=>this.pickups=r.data,error:()=>void Swal.fire('Unable to load today’s pickups','','error')})}}
